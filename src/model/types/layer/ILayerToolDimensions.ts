/**
 * This type provides the specification of the layer tool props model.
 * 
 * @author Jiri Hynek
 */
// eslint-disable-next-line @typescript-eslint/ban-types
type ILayerToolDimensions = {}
// this type will is extended using the intersection operator &
// Record<string, never> can not be used this way
export default ILayerToolDimensions;